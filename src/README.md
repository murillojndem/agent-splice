# Source projects

Bootstrap only the projects required by the active roadmap stage in `docs/ARCHITECTURE.md` and `CLAUDE.md`.

Stage 1 starts with the transparent trace proxy core. Do not create replay, conformance orchestration, evaluation workers, Anthropic translation, or vendor-specific adapter projects until the roadmap task explicitly reaches those stages.

Do not place implementation directly in this directory.
